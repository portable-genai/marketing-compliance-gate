"""Adapters for the ``live`` laptop profile: the local open-weight model behind the model port.

Every other port under ``live`` binds the same offline adapter ``local`` does, so this package
holds only what differs.
"""
